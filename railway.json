{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput --settings=config.settings.prod"
  },
  "deploy": {
    "preDeployCommand": "python manage.py migrate --noinput --settings=config.settings.prod && python manage.py seed --if-empty --count 10000 --settings=config.settings.prod",
    "startCommand": "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --access-logfile -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
