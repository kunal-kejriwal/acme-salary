# Local development: everything production runs, plus the test tooling.
#
# Includes the production manifest rather than restating it, so the two
# cannot drift. The nested include is fine here -- pip resolves it locally,
# and no deployment builder reads this file.

-r ../requirements.txt

pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
