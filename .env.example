# Copy to .env and adjust. Never commit .env.

# --- Django ---------------------------------------------------------------
DJANGO_SETTINGS_MODULE=config.settings.dev
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# --- Database -------------------------------------------------------------
# Optional. Unset, development falls back to a local SQLite file (db.sqlite3),
# so the project runs with no external services. Point this at Postgres when
# you have one; production requires it.
# DATABASE_URL=postgres://acme:acme@localhost:5432/acme_salary

# --- CORS and session cookies ----------------------------------------------
# The Vite dev server origin.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Origins allowed to send unsafe requests. Needed once the SPA is deployed on
# a different host from the API.
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Cross-site cookies need SameSite=None AND Secure=True together. Leave these
# as the lax defaults for local development over http.
# SESSION_COOKIE_SAMESITE=None
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SAMESITE=None
# CSRF_COOKIE_SECURE=True

# --- Frontend -------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:8000/api/v1
